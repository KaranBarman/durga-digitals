import { LucideProps } from "lucide-react";
import Image from "next/image";

export const Icons = {
  logo: (props: LucideProps) => (
    <Image src={"/Untitled design.png"} alt="logo" width={60} height={60} />
  ),
};
